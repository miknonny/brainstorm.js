// Solving this problem recursively.

var locate = function (arr, value) {

};

// Alogrithm.

// Something that terminates this array.
// processing problems that explore several branches of which each might
// branch out into more branches.
console.log([].length);